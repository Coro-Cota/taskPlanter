add the ability to edit task
change numeral priority with arbitrary words such as "low" "medium" and "high"
Make it so you dont have to add a priority, but it will automatically start at the lowest
add images
make a complete button for tasks
when complete make it so the image of the plant moves to the next image